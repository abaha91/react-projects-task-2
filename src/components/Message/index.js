import React from 'react';
import { link } from 'fs';

class Message extends React.Component {
    render () {
        return (
            <div>
            </div>
        );
    }
}

export default Message;
